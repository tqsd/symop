r"""Device action definitions.

Provides the core data structures used to represent semantic device
actions produced during the planning stage.

A device action captures the device kind, bound ports, execution
parameters, and any label edits required after applying the device.
It serves as the interface between semantic device models and the
runtime kernel execution layer.
"""
